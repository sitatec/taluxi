import 'package:flutter/material.dart';

class SlashPage extends StatelessWidget {
  const SlashPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('SlashPage'),
      ),
    );
  }
}
